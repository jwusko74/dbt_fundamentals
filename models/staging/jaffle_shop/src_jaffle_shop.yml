version: 2

sources:
  - name: jaffle_shop
    description: A clone of a Postgres application database.
    database: raw
    schema: jaffle_shop
    tables: 
      - name: customers
        description: Raw customer data.
        columns:
        - name: id
          description: Primary key for customers data.
          tests:
          - unique
          - not_null
          description: Test uniqueness and if values are null in the id field

      - name: orders
        description: Raw orders data.
        columns:
        - name: id
          description: Primary key for orders data.
          tests:
          - unique
          - not_null
        loaded_at_field: _etl_loaded_at
        description: Load date of orders data.
        freshness:
          warn_after: {count: 12, period: hour}
          error_after: {count: 24, period: hour}
        description: Test the age of data in the source.
